apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sql-pv-claim
  namespace: abdullah-tawazun
  labels:
    app: sql
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi